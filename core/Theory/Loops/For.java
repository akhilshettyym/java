// package MeAndCode.Loops;

// public class For {
// public static void main(String[] args) {
// // Demonstrating while loop
// int a = 0;
// while (a < 5) { // While loop checks condition first
// System.out.println(a);
// a++;
// }

// // Demonstrating do-while loop
// int b = 10; // Even if b starts at 10, the loop executes at least once
// do {
// System.out.println(b);
// b++;
// } while (b < 5);

// // Print first n natural numbers using do-while loop
// int n = 10;
// int i = 1;
// do {
// System.out.println(i);
// i++;
// } while (i <= n);

// // Print odd numbers using for loop
// System.out.println("Odd numbers from 1 to 10:");
// for (int j = 1; j <= 10; j++) {
// if (j % 2 != 0) {
// System.out.println(j + " is an Odd number");
// }
// }
// }
// }

// // While checks the condition first before executing the code.
// // Do-while executes the code at least once before checking the condition.
// // For loop is used when we know the number of iterations in advance. It is
// more efficient than
// // while or do-while loops because it doesn't require a condition check at
// the end of each
// // iteration. It is also more readable because the loop variables are
// declared at the
// // beginning of the loop. It is also more efficient because it doesn't
// require a condition
// // check at the end of each iteration. It is also more readable because the
// loop variables
// // are declared at the beginning of the loop. It is also more efficient
// because it doesn't
// // require a condition check at the end of each iteration. It is also more
// readable because
// // the loop variables are declared at the beginning of the loop. It is also
// more efficient
// // because it doesn't require a condition check at the end of each iteration.